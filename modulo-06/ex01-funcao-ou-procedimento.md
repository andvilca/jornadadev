Str() — converte número em string

Função. Converte os números em uma string, e retorna a string

QOut() — exibe texto na tela

Procedimento. Só imprime um texto

Date() — retorna a data atual

Função. Retorna um dado: a data atual

Len() — retorna o tamanho de uma string ou array

Função. Calcula o tamanho e retorna um valor

Upper() — retorna a string em maiúsculas

Função. Modifica a string original, mudando os caracteres para maiúsculo

ClearScreen() — limpa a tela

Procedimento. Apenas retira o histórico 